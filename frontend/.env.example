# Clerk (same publishable key as your Clerk dashboard)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...

# Backend API — match backend PORT (default local setup uses 3003)
VITE_API_URL=http://localhost:3003/api
